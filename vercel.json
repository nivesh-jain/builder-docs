{
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npm rebuild esbuild && npm run build",
  "outputDirectory": "public",
  "rewrites": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}